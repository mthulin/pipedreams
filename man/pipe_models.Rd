% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models.R
\name{pipe_models}
\alias{pipe_models}
\alias{lm}
\alias{lm.default}
\alias{lm.data.frame}
\alias{glm}
\alias{glm.default}
\alias{glm.data.frame}
\alias{aov}
\alias{aov.default}
\alias{aov.data.frame}
\title{Pipe-friendly wrappers for lm, glm, and aov}
\usage{
lm(x, ...)

\method{lm}{default}(x, ...)

\method{lm}{data.frame}(x, formula, ...)

glm(x, ...)

\method{glm}{default}(x, ...)

\method{glm}{data.frame}(x, formula, ...)

aov(x, ...)

\method{aov}{default}(x, ...)

\method{aov}{data.frame}(x, formula, ...)
}
\arguments{
\item{x}{For \code{data.frame} methods: a data frame to use as the \code{data}
argument. For \code{default} methods: passed straight through to the
corresponding \verb{stats::} function.}

\item{...}{Additional arguments passed to the underlying stats function.}

\item{formula}{A formula, passed to \code{\link[stats:aov]{stats::aov()}}.}
}
\value{
For \code{lm}: an object of class \code{"lm"}, as returned by \code{\link[stats:lm]{stats::lm()}}.

For \code{glm}: an object of class \code{c("glm", "lm")}, as returned by
\code{\link[stats:glm]{stats::glm()}}.

For \code{aov}: an object of class \code{c("aov", "lm")}, as returned by
\code{\link[stats:aov]{stats::aov()}}.
}
\description{
These functions wrap \code{\link[stats:lm]{stats::lm()}}, \code{\link[stats:glm]{stats::glm()}}, and \code{\link[stats:aov]{stats::aov()}} as
proper S3 generics, adding \code{data.frame} methods so that a data frame can be
passed as the first argument via the base pipe (\verb{|>}).
}
\details{
Because \code{lm}, \code{glm}, and \code{aov} in the stats package are not internally S3
generics, they cannot be extended with new methods directly. This package
masks them with generic versions that dispatch on the class of the first
argument. The \code{.default} methods call the original \verb{stats::} functions, so
all existing behaviour is preserved.
}
\examples{
# Without pipedreams:
# mtcars |> lm(mpg ~ wt, data = _)

# With pipedreams:
mtcars |> lm(mpg ~ wt)

mtcars |> glm(am ~ mpg + wt, family = binomial)

npk |> aov(yield ~ block + N * P * K)

}
