% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nls.R
\name{pipe_nls}
\alias{pipe_nls}
\alias{nls}
\alias{nls.default}
\alias{nls.data.frame}
\title{Pipe-friendly wrapper for nls}
\usage{
nls(x, ...)

\method{nls}{default}(x, ...)

\method{nls}{data.frame}(x, formula, ...)
}
\arguments{
\item{x}{For the \code{data.frame} method: a data frame to use as the \code{data}
argument. For the \code{default} method: passed straight through to
\code{\link[stats:nls]{stats::nls()}}.}

\item{...}{Additional arguments passed to \code{\link[stats:nls]{stats::nls()}}.}

\item{formula}{A nonlinear model formula, passed to \code{\link[stats:nls]{stats::nls()}}.}
}
\value{
An object of class \code{"nls"}, as returned by \code{\link[stats:nls]{stats::nls()}}.
}
\description{
Wraps \code{\link[stats:nls]{stats::nls()}} as a proper S3 generic, adding a \code{data.frame} method
so that a data frame can be passed as the first argument via the base pipe
(\verb{|>}).
}
\examples{
# Without pipedreams:
# Puromycin |> subset(state == "treated") |>
#   nls(rate ~ Vm * conc / (K + conc), data = _,
#       start = list(Vm = 200, K = 0.05))

# With pipedreams:
Puromycin |>
  subset(state == "treated") |>
  nls(rate ~ Vm * conc / (K + conc), start = list(Vm = 200, K = 0.05))

}
